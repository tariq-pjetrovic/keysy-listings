generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}

model Listing {
  id     String @id @default(uuid()) @db.Uuid
  make   String
  model  String
  year   Int
  price  Int

  @@map("listings")
}